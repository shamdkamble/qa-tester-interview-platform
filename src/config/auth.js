/** Interviewer / admin access PIN — not shown in the UI */
export const ADMIN_PIN = 'Sdk@9370';

/**
 * Secret used to sign candidate access tokens (admin-generated).
 * Change only if you need to invalidate all outstanding tokens at once.
 */
export const TOKEN_SIGNING_SECRET = 'bughunt-qa-token-v1-Sdk@9370';
